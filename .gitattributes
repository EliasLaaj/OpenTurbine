# Keep source and documentation deterministic across Windows and Linux CI.
* text=auto eol=lf

# This wrapper is launched by Windows Command Prompt.
*.cmd text eol=crlf

# Generated / binary assets — never normalize line endings.
data/*.gz binary
*.gz  binary
*.png binary
